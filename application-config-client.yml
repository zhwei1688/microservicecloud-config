spring:
  profiles:
    active:
    - dev
---
server:
  port: 8201
spring:
  profiles: dev #开发环境
  application:
    name: microsevicecloud-config-client
eureka:
  client:
    service_url:
      defaultZone: http://eureka-dev.com:7001/eureka

---
server:
  port: 8202
spring:
  profiles: test #开发环境
  application:
    name: microsevicecloud-config-client
eureka:
  client:
    service_url:
      defaultZone: http://eureka-dev.com:7001/eureka